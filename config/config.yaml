defaults:  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  
hydra:  
  output_subdir: null  
  run:  
    dir: .

data_preprocess:
  raw_data_path: "../data/solved_problems.pkl"
  num_test: 512
  num_val: 2048

tok_data:
  train_file: "data/ca_train.json"
  val_file: "data/ca_test.json"
  test_file: "data/ca_test.json"
  tokenizer_path: "tokenizer/tokenizer.json"

data:
  datapath: data
  train_file: "ca_train.json"
  val_file: "ca_test.json"
  num_workers: 32
  split_str: "AC-begin"

model:
  name: "Pythia-${model.n_layer}-${model.n_head}-${model.n_embd}-solve"
  batch_size: 64
  accumulate_grad_batches: 1
  block_size: 2048
  epochs: 500
  n_layer: 12
  n_head: 8
  n_embd: 128
  vocab_size: 202
  padded_vocab_size: 202
  bos_id: 199
  eos_id: 203

optim:
  lr: 1e-2

eval:
  num_examples: ${data_preprocess.num_test}
  batch_size: 128
  results_dir: "data/eval_results/${model.name}"

convert_hf:
  in_path: "temp/${model.name}"
  out_path: "temp/hf_${model.name}"

inference:
  modelpath: "./temp/hf_Pythia-${model.n_layer}-${model.n_head}-${model.n_embd}-solve"
  datapath: ${data.datapath}/generalization/
  batch_size: 256