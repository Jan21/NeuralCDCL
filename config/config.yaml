defaults:  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  
hydra:  
  output_subdir: null  
  run:  
    dir: .

data_preprocess:
  raw_data_path: "../data/solved_problems.pkl"
  num_test: 256
  num_val: 2048

tok_data:
  train_file: "data/mixed/mixed_train.json"
  val_file: "data/mixed/mixed_test.json"
  test_file: "data/mixed/mixed_test.json"
  tokenizer_path: "tokenizer/tokenizer.json"

data:
  datapath: data
  train_file: "mixed/mixed_train.json"
  val_file: "mixed/mixed_test.json"
  num_workers: 32
  split_str: "SPLIT_BEGIN"

model:
  name: "Pythia-${model.n_layer}-${model.n_head}-${model.n_embd}-mix"
  batch_size: 128
  accumulate_grad_batches: 1
  block_size: 4096
  epochs: 100
  n_layer: 12
  n_head: 16
  n_embd: 256
  vocab_size: 92
  padded_vocab_size: 92
  bos_id: 86
  eos_id: 90

optim:
  lr: 1e-2

eval:
  num_examples: ${data_preprocess.num_test}
  batch_size: 256
  results_dir: "data/eval_results/${model.name}"

convert_hf:
  in_path: "temp/${model.name}"
  out_path: "temp/hf_${model.name}"

inference:
  modelpath: "./temp/hf_Pythia-12-16-256-mixed"
  datapath: ${data.datapath}/generalization/
  batch_size: 256