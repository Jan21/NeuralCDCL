defaults:  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  
hydra:  
  output_subdir: null  
  run:  
    dir: .

data_preprocess:
  raw_data_path: "../data/solved_problems.pkl"
  num_test: 138
  num_val: 138

tok_data:
  train_file: "temp/cdcl_train.json"
  val_file: "temp/cdcl_test.json"
  tokenizer_path: "tokenizer/tokenizer.json"

data:
  datapath: temp
  train_file: "cdcl_train.json"
  val_file: "cdcl_test.json"
  num_workers: 32
  split_str: "begin"
  
model:
  name: "Pythia-${model.n_layer}-${model.n_head}-${model.n_embd}-clause_select-split_vars-gen"
  batch_size: 128
  accumulate_grad_batches: 1
  block_size: 2048
  epochs: 200
  n_layer: 12
  n_head: 8
  n_embd: 256
  vocab_size: 44
  padded_vocab_size: 44
  bos_id: 38
  eos_id: 42

optim:
  lr: 1e-2

eval:
  num_examples: ${data_preprocess.num_test}
  batch_size: 138
  results_dir: "data/eval_results/${model.name}"

convert_hf:
  in_path: "temp/${model.name}"
  out_path: "temp/hf_${model.name}"

inference:
  modelpath: "./temp/hf_Pythia-12-8-256-clause_select-split_vars-gen"
  datapath: ${data.datapath}/generalization/
  batch_size: 128
