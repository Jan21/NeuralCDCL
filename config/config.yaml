defaults:  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  
hydra:  
  output_subdir: null  
  run:  
    dir: .

data_preprocess:
  raw_data_path: "../data/solved_problems.pkl"
  num_test: 256
  num_val: 2048

tok_data:
  train_file: "data/train_CA.json"
  val_file: "data/test_CA.json"
  test_file: "data/generalization/test_CA_ood_16-25.json"
  tokenizer_path: "tokenizer/tokenizer.json"

data:
  datapath: data
  train_file: "test_CA.json"
  val_file: "test_CA.json"
  num_workers: 32
  split_str: "AC-begin"

model:
  name: "Pythia-${model.n_layer}-${model.n_head}-${model.n_embd}-ca"
  batch_size: 128
  accumulate_grad_batches: 1
  block_size: 2048
  epochs: 100
  n_layer: 12
  n_head: 16
  n_embd: 256
  vocab_size: 80
  padded_vocab_size: 80
  bos_id: 75
  eos_id: 79

optim:
  lr: 1e-2

eval:
  num_examples: ${data_preprocess.num_test}
  batch_size: 256
  results_dir: "data/eval_results/${model.name}"

convert_hf:
  in_path: "temp/${model.name}"
  out_path: "temp/hf_${model.name}"

inference:
  modelpath: "./temp/hf_Pythia-12-16-256-ca"
  datapath: ${data.datapath}/generalization/
  batch_size: 256